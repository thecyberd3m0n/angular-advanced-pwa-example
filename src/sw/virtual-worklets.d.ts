// Virtual module provided by esbuild.sw.mjs at build time. It expands to a
// barrel of side-effect imports for every `src/**/*.sw.ts` worklet so each
// @ServiceWorklet() decorator runs. Declared here so TypeScript/IntelliSense
// recognizes the import in combined.sw.ts.
declare module 'virtual:worklets';
